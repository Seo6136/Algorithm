#include <iostream>
#include <set>
#include <unordred_map>
#include <vector>

using namespace std;

int n,m,t,seq,tt;
unordered_map<pair<int,int>, int> marvels;
vector<vector<int>> visited;

int main() {
    cin >> t;

    for (int tc = 0; tc<t; t++) {
        cin >> n >> m;
        visited = vector<vector<int>> (n, vector<int>(n,0));
        s


    }
    // Please write your code here.
    return 0;
}